# Installation Notes

## Debian/Ubuntu

On Debian GNU/Linux 13 (trixie), the `unrar` package may not be available. The installer will fall back to `unrar-free` when possible or skip rar tooling with a warning so the rest of the install can continue.
